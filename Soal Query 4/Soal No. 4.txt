SELECT DISTINCT dosen.Kd_Dosen, dosen.Nama FROM dosen INNER JOIN jadwal ON dosen.Kd_dosen=jadwal.Kd_Dosen WHERE dosen.Kd_dosen IN (SELECT Kd_dosen FROM jadwal WHERE hari=1) AND dosen.Kd_dosen IN (SELECT Kd_dosen FROM jadwal WHERE hari=3)